<h1>my tasks</h1>
<p>
  <% @tasks.each do |task| %>
  <%= link_to task.title, task_path(task) %>
  <br>
  <%= task.details %>
  <br>
  <%= task.completed? ? "[X]" : "[ ]"%>
  <br>
  <br>
  <% end %>
</p>

<%= link_to "create", new_task_path  %>
